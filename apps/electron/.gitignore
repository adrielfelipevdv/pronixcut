node_modules
dist
*.log
# Generated by scripts/prepare-standalone.js — a build artifact (the
# dereferenced Next.js standalone server), not source. Regenerated by
# `npm run prepare:web` before every `npm run dist`.
resources/
